@using Microsoft.AspNet.Identity

<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        @if (Request.IsAuthenticated)
        {
            using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
            {
                @Html.AntiForgeryToken()
                <ul class="nav metismenu" id="side-menu">
                    <li class="nav-header">
                        <div class="dropdown profile-element">
                            <span>
                                @if (Session["photo"] == null)
                                {
                                    <img alt="image" class="img-circle center-block" src="~/Content/Template/assets/img/avator/default-avatar.png" width="148" height="148" />
                                }
                                else
                                {
                                    <img alt="image" class="img-circle center-block" src="@Session["photo"]" width="148" height="148" />
                                }
                            </span>
                            <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                                <span class="clear">
                                    <span class="block m-t-xs">
                                        <strong class="font-bold">@Session["Name"]</strong>
                                    </span> <span class="text-muted text-xs block">@Session["PIN"]<b class="caret"></b></span>
                                </span>
                            </a>

                            <ul class="dropdown-menu animated fadeInRight m-t-xs">
                                <li>
                                    @if (Session["Id"] == null)
                                    {
                                        <a href="#"><i class="fa fa-image"></i> <span class="nav-label">View Profile</span></a>
                                    }
                                    else
                                    {
                                        <a href="~/iPension/RSAClientPortal/BioData?id=@Session["Id"]"><i class="fa fa-image"></i> <span class="nav-label">View Profile</span></a>
                                    }
                                </li>
                                <li>
                                    @Html.ActionLink("Change Password", "ChangePassword", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
                                </li>
                                <li class="divider"></li>
                                <li><a href="javascript:document.getElementById('logoutForm').submit()">Logout</a></li>
                            </ul>

                        </div>
                        <div class="logo-element">
                            FPML
                        </div>
                    </li>
                    @*<li class="@Html.IsSelected(controller: "Home", action: "Main")">
                        <a href="~/iPension/RSAClientPortal"><i class="fa fa-dashboard"></i> <span class="nav-label">DashBoard</span> </a>
                    </li>*@
                    <li class="@Html.IsSelected(controller: "Home", action: "Minor")">
                        @if (Session["Id"] == null)
                        {
                            <a href="#"><i class="fa fa-image"></i> <span class="nav-label">View Profile</span></a>
                        }
                        else
                        {
                            <a href="~/iPension/RSAClientPortal/BioData?id=@Session["Id"]"><i class="fa fa-image"></i> <span class="nav-label">View Profile</span></a>
                        }
                    </li>
                    @*<li class="@Html.IsSelected(controller: "Home", action: "Main")">
                        <a href="~/iPension/RSAClientPortal/WelcomeLetter"><i class="fa fa-th-large"></i> <span class="nav-label">Registration Certificate</span></a>
                    </li>*@
                    <li class="@Html.IsSelected(controller: "Home", action: "Main")">
                        <a href="~/iPension/RSA/RecaptureForm"><i class="fa fa-th-large"></i> <span class="nav-label">Data Recapture</span> @*<span class="label label-primary float-right">Data Recapture</span>*@</a>
                    </li>

                 @*   <li class="@Html.IsSelected(controller: "Home", action: "Minor")">
         <a href="~/iPension/RSAClientPortal/AvcSubscription"><i class="fa fa-th-large"></i> <span class="nav-label">AVC Subscription</span></a>
     </li>
    <li class="@Html.IsSelected(controller: "Home", action: "Minor")">
         <a href="#"><i class="fa fa-th-large"></i> <span class="nav-label">Apply for Benefits</span></a>
     </li>

     <li class="@Html.IsSelected(controller: "Dashboards")">
         <a href="@Url.Action("#", "#")"><i class="fa fa-th-large"></i> <span class="nav-label" data-i18n="nav.dashboard">Track Benefits Applications</span> <span class="fa arrow"></span></a>
         <ul class="nav nav-second-level collapse @Html.IsSelected(controller: "#", cssClass: "in")">
             <li class="@Html.IsSelected(action: "#")"><a href="@Url.Action("#", "#")">Submitted Applications</a></li>
             <li class="@Html.IsSelected(action: "#")"><a href="@Url.Action("#", "#")">Pensing Online Applications</a></li>

         </ul>
     </li>
     <li class="@Html.IsSelected(controller: "Home", action: "Minor")">
         <a href="#"><i class="fa fa-th-large"></i> <span class="nav-label">Messaging</span></a>
     </li>

     <li class="@Html.IsSelected(controller: "Home", action: "Minor")">
         <a href="#"><i class="fa fa-th-large"></i> <span class="nav-label">Advisory Service</span></a>
     </li>
     <li class="@Html.IsSelected(controller: "Home", action: "Minor")">
         <a href="#"><i class="fa fa-th-large"></i> <span class="nav-label">FAQ</span></a>
     </li>*@
                </ul>
            }
        }
    </div>
</nav>